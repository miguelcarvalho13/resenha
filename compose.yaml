services:

  db:
    image: postgres:17.6
    restart: always
    shm_size: 128mb
    environment:
      POSTGRES_PASSWORD: example
    volumes:
      - db:/var/lib/postgresql/data

  adminer:
    image: adminer:5.4.0
    restart: always
    ports:
      - 127.0.0.1:8585:8080

volumes:
  db: